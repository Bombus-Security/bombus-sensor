#!/bin/bash

#This script will create a software agent on the host

if ! [ $(id -u) = 0 ]; then
        echo "Must be run as root."
        exit 1
fi

#Install ssh-client and scp

#Install docker repos

#Install docker

#Install python and pip

#Install pip docker

#Create service user

#Add user to docker group

#Create ssh keys. This will require a cert request

#ssh-copy-id to HQ

#What about the HQ's key?
